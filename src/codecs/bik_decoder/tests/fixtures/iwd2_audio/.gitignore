# Raw PCM dumps are generated by ../gen_iwd2_audio_fixtures.py — they're
# bulky (~50 MB total) and trivially regenerable from any FFmpeg install,
# so we don't commit them. The corresponding sha256 manifest sits at
# ../iwd2_audio.txt and IS committed.
*.s16le
